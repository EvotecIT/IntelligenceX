# TODO

- Implement `IntelligenceX.Tools.EventLog` EVTX parsing tools (EventViewerX) with safe-by-default limits and structured JSON output.
- Implement `IntelligenceX.Tools.ADPlayground` query tools (ADPlayground) with safe-by-default limits and structured JSON output.
- Decide packaging split for Windows-only capabilities (separate `net*-windows` pack vs runtime OS checks).
