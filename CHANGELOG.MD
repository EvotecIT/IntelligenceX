# Changelog

## Unreleased
- IntelligenceX.Chat.Host now defaults to parallel tool calls (`ParallelToolCalls = true`).
  Use `--no-parallel-tools` to force serial tool execution.

## 0.1.0
- Initial library scaffold.
