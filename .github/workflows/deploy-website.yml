name: Deploy Website

on:
  schedule:
    - cron: "17 4 * * 1"
  push:
    branches: [master]
    paths:
      - 'Website/**'
      - 'Docs/**'
      - '.github/workflows/deploy-website.yml'
  workflow_dispatch:

permissions:
  actions: write
  contents: read
  packages: read
  pages: write
  id-token: write

jobs:
  website-deploy:
    uses: EvotecIT/PSPublishModule/.github/workflows/powerforge-website-deploy.yml@7db91a60d5c7b3a2c63e55c7c3d2851bda4f831a
    with:
      website_root: Website
      pipeline_config: Website/pipeline.json
      site_output_path: Website/_site
      post_deploy_pipeline_config: Website/pipeline.cloudflare.json
      manage_cloudflare_site_policy: true
      engine_mode: source
      runner_labels_json: ${{ (github.event.repository.private && vars.IX_FORCE_GITHUB_HOSTED != 'true') && '["self-hosted","linux"]' || '["ubuntu-latest"]' }}
      powerforge_repository: EvotecIT/PSPublishModule
      powerforge_ref: 7db91a60d5c7b3a2c63e55c7c3d2851bda4f831a
    secrets:
      indexnow_key: ${{ secrets.INDEXNOW_KEY }}
      cloudflare_api_token: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      cloudflare_zone_id: ${{ secrets.CLOUDFLARE_ZONE_ID_IX }}
