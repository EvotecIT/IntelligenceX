name: Website Build (PR)

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  website-build:
    uses: EvotecIT/PSPublishModule/.github/workflows/powerforge-website-ci.yml@53d00f50bc525a6e2e4163cb80170d61c815432c
    with:
      website_root: Website
      pipeline_config: Website/pipeline.json
      engine_mode: binary
      runner_labels_json: ${{ (github.event.repository.private && vars.IX_FORCE_GITHUB_HOSTED != 'true') && '["self-hosted","linux"]' || '["ubuntu-latest"]' }}
      powerforge_repository: EvotecIT/PSPublishModule
      powerforge_ref: 53d00f50bc525a6e2e4163cb80170d61c815432c
      powerforge_tool_lock_path: Website/.powerforge/tool-lock.json
