name: Website Build (PR)

on:
  pull_request:
  workflow_dispatch:

concurrency:
  group: website-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  website-build:
    uses: EvotecIT/PSPublishModule/.github/workflows/powerforge-website-ci.yml@ba663dbeaaca2be3d8ad176c6e14075cb8222187
    with:
      website_root: Website
      pipeline_config: Website/pipeline.json
      runner_labels_json: ${{ (github.event.repository.private && vars.IX_FORCE_GITHUB_HOSTED != 'true') && '["self-hosted","linux"]' || '["ubuntu-latest"]' }}
      powerforge_repository: EvotecIT/PSPublishModule
      powerforge_ref: ba663dbeaaca2be3d8ad176c6e14075cb8222187
      report_artifact_name: powerforge-website-reports
    secrets: inherit
