name: Self-Hosted Runner Housekeeping

on:
  schedule:
    - cron: '17 */6 * * *'
  workflow_run:
    workflows:
      - Test .NET Libraries
      - IntelligenceX Review
      - Claude Code Review
      - Analysis Catalog Guardrail
    types:
      - completed
  workflow_dispatch:
    inputs:
      min_free_gb:
        description: 'Minimum free disk (GiB) required after cleanup'
        required: false
        default: '20'

permissions:
  contents: read

concurrency:
  group: self-hosted-runner-housekeeping-${{ github.repository }}
  cancel-in-progress: false

jobs:
  cleanup-linux-runner:
    if: ${{ github.event.repository.private }}
    uses: EvotecIT/PSPublishModule/.github/workflows/powerforge-github-runner-housekeeping.yml@8cd12880dbf23918a9058997f9307527aa4eebc5
    with:
      config-path: ./.powerforge/runner-housekeeping.json
      apply: true
      powerforge-ref: 8cd12880dbf23918a9058997f9307527aa4eebc5
      runner-labels: '["self-hosted","ubuntu"]'
      runner-min-free-gb: ${{ github.event_name == 'workflow_dispatch' && inputs.min_free_gb || vars.IX_RUNNER_HOUSEKEEPING_MIN_FREE_GB }}
      report-artifact-name: runner-housekeeping-reports
    secrets:
      github-token: ${{ secrets.GITHUB_TOKEN }}
